export default function Header() {
  return (
    <div
      className="fixed top-0 w-screen flex justify-between items-center z-[101]"
      style={{
        paddingLeft: "clamp(10px, 1.042vw, 20px)",
        paddingTop: "clamp(10px, 1.042vw, 20px)",
      }}
    >
      MATTEOBEU
    </div>
  );
}
